export class UserEntity {
    constructor (
      id: number,
      username: string,
      role: string,
      level: number,
      // eslint-disable-next-line camelcase
      access_token: string) {}
  }
  